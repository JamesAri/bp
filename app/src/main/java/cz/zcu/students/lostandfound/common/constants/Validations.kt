package cz.zcu.students.lostandfound.common.constants

/** Validation specific constants. */
class Validations {
    companion object {
        /** Maximal length of phone number. */
        const val MAX_PHONE_NUMBER_LENGTH = 13

        /** Maximal length of lost item filter term. */
        const val MAX_SEARCH_TERM_LENGTH = 25
    }
}